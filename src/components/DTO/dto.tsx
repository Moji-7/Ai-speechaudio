export type TextContent = {
    id: number;
    title: string;
    content: string;
  };
  
  // Define the type for the sentence object
  export type Sentence = {
    id: number;
    text: string;
  };